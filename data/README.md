Fashion-MNIST gets downloaded here automatically the first time you run Stage B
(`datasets.FashionMNIST(root="./data", download=True, ...)`). If your instructor
hands out a pre-downloaded cache before the lab, drop its contents directly into
this folder instead of downloading again.

Don't commit this folder's contents to your submission repo - add `data/` to
your `.gitignore`.
